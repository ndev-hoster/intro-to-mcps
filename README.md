# Intro To MCPs
A repo for me to learn about mcps and build some on the way


# How to link MCP servers to claude desktop

1. Install claude desktop APPIMAGE version from you package manager
2. Find `claude_desktop_config.json` (should be in `~/.config/Claude`)
3. Launch the App Image, the server should show up